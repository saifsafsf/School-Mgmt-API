version: '3.8'

services:
  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: fsdbsdmsd
      MYSQL_DATABASE: school
      MYSQL_USER: root
      MYSQL_PASSWORD: fsdbsdmsd
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  myapp:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - mysql
    env_file:
      - /.env

volumes:
  mysql_data:
